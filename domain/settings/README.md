# :domain:settings

Owner: Android Vibe Design maintainers. Provides application-facing AI-provider and
appearance-preference operations for settings UI. It is an Android library so it can
consume the Android-backed settings data module, but its source API remains free of
Android, UI, resources, and provider SDK types.
