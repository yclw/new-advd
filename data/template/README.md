# :data:template

Owner: Android Vibe Design maintainers. Owns `TemplateRepository`, packaged assets,
and future remote sources. The Repository contract is public to domain; source
adapters and Hilt bindings remain `internal` in this module.
