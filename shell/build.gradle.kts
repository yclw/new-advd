plugins {
    id("avd.android.application")
    id("avd.testing")
}

android {
    namespace = "com.aeibi.avd.shell"

    defaultConfig {
        applicationId = "com.aeibi.avd.shell"
        versionCode = 1
        versionName = "0.1.0"
    }
}
